/*export interface Vendor {
  access_token: string;
  id: string;
  name: string;
  desc: string;
  cuisine: string;
  images?: string[];
  menu?:any[];
  hours:Object;
  stats:{
    likes:number;
    visits:number;};
  nextPageToken?: string;
  viewedPlaylist?: string;}
export interface VendorState {vendors:Vendor[];}
export const initialState: VendorState = {vendors:[]};*/